# common package

This package contains integration event code to be shared between different services, to be specific:

- RabbitMQ integration event bus (`rabbitmq` submodule)
- Integration events of the services (`contracts` submodule)
